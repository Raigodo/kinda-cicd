name: Print Hello World
on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string
      environment:
        required: true
        type: string
      dockerfile:
        required: true
        type: string
      tag:
        required: true
        type: string

env:
  ENVIRONMENT: ${{github.workspace}}/${{inputs.environemt}}

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print Message
        run: echo "${{ inputs.message }}"
